class Dropp {
  constructor(_details = {}) {
    this.id = _details.id;
    this.hasMedia = _details.hasMedia;
    this.username = _details.username;
    this.timestamp = _details.timestamp;
  }
}

module.exports = Dropp;
